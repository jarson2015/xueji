# 学迹 · 手机抽屉高度统一验收 · P1

承接 HANDTEST「手机抽屉高度统一为 `--drawer-phone`」。

## 开发理由

1. 零花/打卡/约定/愿望/任务编辑等抽屉已用 `var(--drawer-phone)`，缺回归锁，改版易退回固定 `70%`/`400px`。  
2. 用源码扫描断言所有 `el-drawer` 在 `isPhone` 分支使用该变量；CSS 定义存在。  
3. 非目标：不改桌面/TV 尺寸；不做全机型视觉像素比对。

## 总览

| ID | 项 | 状态 |
|----|-----|------|
| **P.1** | vitest：扫描 Vue + `--drawer-phone` 定义 | 已完成 |
| **P.2** | e2e：窄屏打开一抽屉，尺寸含 drawer-phone | 已完成 |
| **P.3** | HANDTEST 勾选 | 已完成 |

## 人手（仍必做）

- [ ] 真机窄屏点零花/打卡/愿望抽屉，高度观感一致  

## 验收（本轨自动化）

- [x] drawer-phone unit 通过  
- [x] e2e 窄屏抽屉通过  
- [x] HANDTEST 已勾  
