# 学迹 · 离线打卡验收 · P1

承接 HANDTEST「离线打卡队列」未勾项。  
原则：**e2e 验证顶栏可见 + 入队；单测已覆盖丢弃；真断网手测仍保留一条。**

状态：**已落地**

## 开发理由

1. 离线队列逻辑已有 unit，但顶栏与学生壳联动无人测/无 e2e。  
2. Playwright 可注入 `localStorage` 队列并断言「N 条打卡待同步」。  
3. 非目标：不模拟完整魔数上传离线；不改队列协议。

## 总览

| ID | 项 | 状态 |
|----|-----|------|
| **O.1** | e2e：学生登录后注入队列 → 顶栏显示待同步条数 | ✅ |
| **O.2** | 单测：`removeOfflineCheckin` / 空 flush | ✅ |
| **O.3** | HANDTEST 勾自动化项；人手保留「真断网打卡」 | ✅ |

## 验收

- [x] `test:unit` + `test:e2e` 含离线条通过  
- [x] HANDTEST 离线区部分勾选  
