# 家长今日看板 · P4 开发计划

承接 [`MONITOR_P3_PLAN.md`](./MONITOR_P3_PLAN.md) 与看板 UX 规划中的 **P4 动态流合并** 层。  
原则：**一条时间线、筛选联动；卡片内最多 1 条脚注；不再重复扫两遍**。

状态：**已落地**

---

## 总览

| ID | 项 | 状态 | 说明 |
|----|-----|------|------|
| **P4.1** | 移除卡片内「今日动态」 | ✅ | 删除 `child-timeline` 整块列表 |
| **P4.2** | 卡片脚注（最近 1 条） | ✅ | 「全部」视角下，每孩卡底一行「刚刚 · …」 |
| **P4.3** | 页级「家庭动态」统一流 | ✅ | 合并原「最近动态」；类型标签 + 摘要 |
| **P4.4** | 动态流展开 | ✅ | 默认 5 条，可展开至 12；随筛选重置 |

---

## 信息层级（P4 后 · 看板 UX 闭环）

```
页头
→ 待确认（P0）
→ 家庭总览 + 筛选（P2）
→ 孩子卡：待完成 / 已完成 / 缓做 / 轻干预（P1/P3）
→ 【P4】刚刚 · 脚注（仅「全部」）
→ 【P4】家庭动态（单流）
→ 【P5】洞察条（见 [`MONITOR_P5_PLAN.md`](./MONITOR_P5_PLAN.md)）
```

---

## 总验收

- [x] 卡片内无「今日动态」列表
- [x] 多孩「全部」时，每卡最多 1 行脚注
- [x] 页底仅一条「家庭动态」，随 `childFilterId` 过滤
- [x] 默认 5 条，可展开至 12 条
- [x] `npm run build`（apps/web）通过

---

## 后续

- P5：见 [`MONITOR_P5_PLAN.md`](./MONITOR_P5_PLAN.md)