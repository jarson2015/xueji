# 学迹 · Monitor 孩子卡拆分 · P1

承接行动区 / 洞察条拆分；继续缩小 `MonitorView` 主模板。

## 开发理由

1. 孩子卡模板占看板主体，与展开/缓做/轻提醒/脚注缠在一起，改一处易漏 TV 分支。  
2. 抽 `MonitorChildCard`：任务列表辅助函数随卡内聚；父页只传状态与 emit。  
3. 非目标：不改数据协议；不拆右栏家庭动态列表。

## 总览

| ID | 项 | 状态 |
|----|-----|------|
| **C.1** | 新建 `MonitorChildCard.vue`（含任务切片/状态标签） | 已完成 |
| **C.2** | `MonitorView` 改用组件；相关样式迁入 | 已完成 |
| **C.3** | `build` + e2e（看板待处理）通过 | 已完成 |

## 验收

- [x] 手机/桌面孩子卡展示与操作同前（结构迁入 `MonitorChildCard`，父页 props/emit）
- [x] `npm run build` 通过；e2e「家长看板有待处理」通过（其余失败为登录限流/学生码，与本拆分无关）
