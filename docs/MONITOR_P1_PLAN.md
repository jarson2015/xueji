# 家长今日看板 · P1 开发计划

承接 [`MONITOR_P0_PLAN.md`](./MONITOR_P0_PLAN.md) 与看板 UX 规划中的 **P1 谁 + 未完成什么** 层。  
原则：**2 孩即可切换视角；任务卡首屏只露待完成；已完成默认折叠；筛选联动动态流**。

状态：**已落地**

---

## 总览

| ID | 项 | 状态 | 说明 |
|----|-----|------|------|
| **P1.1** | 2 孩起显示筛选 | ✅ | `showMultiChildFilter`：`length >= 2` |
| **P1.2** | 默认只露未完成 | ✅ | `activeTasks` 过滤 `done`，折叠态最多 5 条 |
| **P1.3** | 已完成折叠区 | ✅ | 「今日已完成 n 件」手风琴，默认收起 |
| **P1.4** | 最近动态联动筛选 | ✅ | `recentFeed` 随 `childFilterId` 过滤 |

---

## 信息层级（P1 后）

```
页头
→ 待确认（P0）
→ 家庭总览
→ 洞察横幅（P5）
→ 【P1】孩子筛选（≥2 孩）
→ 孩子卡：待完成列表 → 已完成折叠 → 缓做 / 动态
→ 【P1】最近动态（随筛选）
```

---

## 总验收

- [x] 2 孩家庭可见「全部 / 各孩」筛选
- [x] 孩子卡默认只显示未完成项（最多 5 条）
- [x] 已完成项收在「今日已完成 n 件」，默认折叠
- [x] 筛选某孩时，页底「最近动态」仅该孩
- [x] `npm run build`（apps/web）通过

---

## 后续（P4+，本阶段不做）

- P4：合并卡片内「今日动态」与页底「最近动态」为单一流
