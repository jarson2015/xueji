# 教育向动机优化 · P1–P2 开发计划

承接 [`EDU_MOTIVATION_PLAN.md`](./EDU_MOTIVATION_PLAN.md)。  
原则：先核对 P1 漏项，再落地 P2；少造表、默认值与软提示优先。

状态：已落地

---

## P1 核对（已落地 / 本轮补齐）

| ID | 项 | 状态 | 本轮动作 |
|----|-----|------|----------|
| P1.1 | 多孩家务默认轮值 | ✅ 已有 | 回归确认即可 |
| P1.2 | 监控去比较化 | ✅ 已有 | — |
| P1.3 | 低龄积分约定护栏 | ✅ 已补齐 | 家庭默认或任一孩为 `young` 时：前端禁开 + API `BadRequest`；切到 young 时强制关约定 |
| P1.4 | per-child `ageBand` | ✅ 已有（迁移 0018） | — |

---

## P2 — 本轮落地

| ID | 项 | 落地要点 | 状态 |
|----|-----|----------|------|
| P2.1 | 愿望默认偏陪伴/体验；金手指二次确认 | DB/实体 `kind` 默认 `experience`；创建/编辑金手指与学生兑换均二次确认 | ✅ |
| P2.2 | 兑换超时未兑现 → 家长软提示 | `/dashboard/summary` 统计 pending >3 天；`MonitorView` 横幅跳转愿望 | ✅ |
| P2.3 | makeup 默认折扣下调 + 文案 | 新家庭默认 **50%**；说明偏「生病/特殊收尾」 | ✅ |
| P2.4 | 积分淡出半自动（可撤销） | 约定页预选建议模式；保存才生效；可改回 | ✅ |

### P2 非目标
- 强制 GET 时改写 `rewardMode`（避免无感改配置）
- 新建「生病」枚举表（文案层足够）
- 学生端隐藏兄妹余额的大改（可 P3）

---

## 迁移

| 迁移 | 内容 |
|------|------|
| `0019` | `wish_items.kind` 默认 → `experience`；`family_settings.makeup_discount_percent` 默认 → 50（仅 default，不改历史行） |

---

## 验收

- [x] `young`（家庭或任一孩）无法打开积分约定，或保存被拦截并提示
- [x] 新建愿望默认 kind=体验/陪伴导向；金手指有二次确认
- [x] 家长监控：有超过 3 天未兑现兑换时出现软提示
- [x] 新家庭补进度默认 50%；文案含「特殊情况收尾」
- [x] 约定页在淡出条件满足时预选 random/weekly_digest，可改回 always 再保存
