# 学迹 · 家长封面对齐 + 离线顶栏 + 文案收口（P0）

承接学生储蓄封面落地后的表面缺口，以及入口减阻遗留的运维口吻、离线队列可见性。  
原则：**补齐已有能力的展示；顶栏与队列同源刷新；文案白话但不隐瞒前提。**

状态：**已落地**

---

## 方向理由

| 理由 | 说明 |
|------|------|
| 家长审目标 | 孩子已上传封面，家长折叠区只见标题/进度 → 「能力有、表面无」 |
| 离线打卡 | 入队有 toast，顶栏只在 mount/online 读数 → 常看不见待同步 |
| 自动确认说明 | 「学迹后台要开着」仍像运维话，与减阻轨不一致 |

---

## 总览

| ID | 项 | 状态 | 开发理由 |
|----|-----|------|----------|
| **B.1** | 家长零花「储蓄目标」展示 `coverUrl` | ✅ | 亲子同频看见目标 |
| **B.2** | 离线队列变更 bump → 顶栏计数；满重试丢弃可感知 | ✅ | 孩子知道「还在本地」 |
| **B.3** | FamilyEdu 自动确认文案去运维口吻 | ✅ | 减阻收尾 |

### 非目标

- 家长代上传封面、流水配图、Redis、登录码 8 位、扩 SoftPrompt  

---

## 改动摘要

- `parent/AllowanceView.vue`：目标行展示封面缩略图  
- `offlineCheckinQueue.ts`：`offlineQueueTick`；flush 返回 `dropped`  
- `StudentLayout.vue`：watch tick + storage；丢弃提示  
- `FamilyEduView.vue`：「本机学迹要保持运行」  
- `HANDTEST_REGRESSION_MASTER.md`：补家长封面与离线手测  

---

## 验收

- [x] 家长零花折叠「储蓄目标」有封面时可见缩略图  
- [x] 离线入队后顶栏立即出现待同步条数  
- [x] 同步失败满次丢弃时有提示（非静默）  
- [x] 自动确认提示无「后台 / API」口吻  
- [x] web `test:unit` + `build`（落地后跑）  
- [x] 本文档 **已落地**
