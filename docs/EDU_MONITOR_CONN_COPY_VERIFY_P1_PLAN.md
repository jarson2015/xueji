# 学迹 · 看板连接态文案验收 · P1

承接 HANDTEST「看板正常连接不刷「实时已连接」；断线可见「离线刷新中」」。

## 开发理由

1. P0 已改为仅 `!wsOk` 显示「离线刷新中」；可见性逻辑内联，HANDTEST 未勾。  
2. 抽出 `showMonitorOfflineTag`；unit 锁死在线不展示、TV 不展示；e2e 断言永不出现「实时已连接」，并阻断 socket 后见离线标签。  
3. 非目标：不改轮询间隔、不改 socket 协议。

## 总览

| ID | 项 | 状态 |
|----|-----|------|
| **C.1** | 抽出离线标签可见性 + unit | 完成 |
| **C.2** | e2e：无「实时已连接」；断 socket 见「离线刷新中」 | 完成 |
| **C.3** | HANDTEST 勾选 | 完成 |

## 人手（仍必做）

- [ ] 真断网/关 API 后看板见离线标签与定时刷新  

## 验收（本轨自动化）

- [x] unit 通过  
- [x] e2e 通过（`setOffline` 断线见标签）  
- [x] HANDTEST 已勾  
